{
  "name": "bradhacker.github.io",
  "version": "0.1.0",
  "private": false,
  "homepage": "/",
  "dependencies": {
    "react": "^16.7.0",
    "react-dom": "^16.7.0",
    "react-modal": "^3.4.4",
    "react-router-dom": "^4.2.2",
    "react-scripts": "^2.1.3"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build -b master -m",
    "gh-help": "gh-pages -h",
    "deploy-nomessage": "gh-pages -d build -b master"
  },
  "devDependencies": {
    "eslint-config-google": "^0.11.0",
    "gh-pages": "^2.0.1"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
